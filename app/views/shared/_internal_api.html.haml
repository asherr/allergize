#searchapi{ :style => "display: none;" }
  = "#{search_url}?name=%QUERY"
#menushow{ :style => "display: none;" }
  -# Hacky way of making show url from index url for menus (via js append)
  = organizations_url
